services:
  db:
    container_name: "db"
    image: mysql:8.0.33
    environment:
      - MYSQL_ROOT_PASSWORD=S#nQ^7qP@yH5
      - TZ=America/Lima
    volumes:
      - /data-bd:/var/lib/mysql
      - /usr/share/zoneinfo/America/Lima:/etc/localtime:ro
      - /usr/share/zoneinfo:/usr/share/zoneinfo:ro
    ports:
      - "3306:3306"
    restart: always
    networks:
      mintec-networks:
        ipv4_address: 172.10.0.25

networks:
  mintec-networks:
    driver: bridge
    ipam:
      config:
        - subnet: 172.10.0.0/24
